name: Governed semantic plan model selection

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: governed-semantic-plan-model-selection-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  guard:
    name: Require trusted main dispatch
    runs-on: ubuntu-latest
    steps:
      - name: Reject non-main workflow refs
        shell: bash
        run: |
          set -euo pipefail
          if [[ "$GITHUB_REF" != "refs/heads/main" ]]; then
            echo "This workflow must be dispatched from the main branch." >&2
            exit 1
          fi

  prepare:
    name: Freeze semantic model-selection corpus
    needs: guard
    runs-on: ubuntu-latest
    outputs:
      trusted_sha: ${{ steps.trusted.outputs.sha }}
    steps:
      - name: Check out trusted main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: false

      - name: Record trusted main commit
        id: trusted
        shell: bash
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install runtime dependency
        run: pip install pyyaml

      - name: Validate and freeze the corpus
        shell: bash
        run: |
          set -euo pipefail
          python -m llm_analysis.semantic_plan_model_evaluation prepare \
            --repository-root . \
            --config config/semantic-plan-model-selection.yml \
            --output-dir "$RUNNER_TEMP/semantic-plan-model-selection-prepared"

      - name: Upload prepared corpus
        uses: actions/upload-artifact@v4
        with:
          name: semantic-plan-model-selection-prepared-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/semantic-plan-model-selection-prepared/
          if-no-files-found: error
          retention-days: 7

  evaluate:
    name: Run protected semantic model comparison
    needs: prepare
    runs-on: ubuntu-latest
    environment: governed-llm-dry-run
    steps:
      - name: Check out exact trusted main commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.trusted_sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify trusted commit
        env:
          TRUSTED_SHA: ${{ needs.prepare.outputs.trusted_sha }}
        shell: bash
        run: test "$(git rev-parse HEAD)" = "$TRUSTED_SHA"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install runtime dependency
        run: pip install pyyaml

      - name: Download prepared corpus
        uses: actions/download-artifact@v4
        with:
          name: semantic-plan-model-selection-prepared-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/semantic-plan-model-selection-prepared

      - name: Run bounded model selection
        id: evaluation
        continue-on-error: true
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TRUSTED_SHA: ${{ needs.prepare.outputs.trusted_sha }}
        shell: bash
        run: |
          set -euo pipefail
          python -m llm_analysis.semantic_plan_model_evaluation run \
            --repository-root . \
            --config config/semantic-plan-model-selection.yml \
            --prepared-dir "$RUNNER_TEMP/semantic-plan-model-selection-prepared" \
            --output-dir "$RUNNER_TEMP/semantic-plan-model-selection" \
            --trusted-main-sha "$TRUSTED_SHA"

      - name: Publish evaluation summary
        if: always()
        shell: bash
        run: |
          if [[ -f "$RUNNER_TEMP/semantic-plan-model-selection/actions-summary.md" ]]; then
            cat "$RUNNER_TEMP/semantic-plan-model-selection/actions-summary.md" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "# Semantic claim-plan model selection" >> "$GITHUB_STEP_SUMMARY"
            echo "The evaluation failed before a complete diagnostic summary was produced." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload protected evaluation artefacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: semantic-plan-model-selection-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/semantic-plan-model-selection/
          if-no-files-found: warn
          retention-days: 30

      - name: Fail when evaluation infrastructure failed
        if: steps.evaluation.outcome != 'success'
        shell: bash
        run: |
          echo "The controlled model selection did not complete. Review retained diagnostics." >&2
          exit 1
